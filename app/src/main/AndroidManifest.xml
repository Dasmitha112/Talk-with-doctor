<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.talk_with_doctor">
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Bookings"></activity>
        <activity android:name=".orderMedicines" />
        <activity android:name=".notificationsPatient" />
        <activity android:name=".SignUpPatient" />
        <activity android:name=".patientDetails" />
        <activity android:name=".doctorDetails" />
        <activity android:name=".newAppointment" />
        <activity android:name=".profilePatient" />
        <activity android:name=".homePatient" />
        <activity android:name=".Logout" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>