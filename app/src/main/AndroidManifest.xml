<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.talk_with_doctor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".viewPharmacy"></activity>
        <activity android:name=".viewPatient" />
        <activity android:name=".viewDoctor" />
        <activity android:name=".requestEmail" />
        <activity android:name=".homeAdmin" />
        <activity android:name=".addPharmacy" />
        <activity android:name=".addDoctor" />
        <activity android:name=".profileAdmin" />
        <activity android:name=".Logout" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>